/**
 * Interface for the sidenav top buttons
 * @param href: string - the link to the page
 * @param iconPath: string - the path to the icon
 * @param text: string - the text to display under the icon
 */
export interface SideButtonProps {
  href: string;
  iconPath: string;
  text: string;
}
